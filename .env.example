# FRW Docker Environment Configuration
# Copy this file to .env and customize as needed

# ============================================
# IPFS Configuration
# ============================================

# IPFS profile (server, lowpower, test)
IPFS_PROFILE=server

# IPFS network ports
IPFS_SWARM_PORT=4001
IPFS_API_PORT=5001
IPFS_GATEWAY_PORT=8080

# IPFS routing mode (dht, dhtclient, none)
IPFS_ROUTING=dhtclient

# Maximum storage for IPFS (e.g., 10GB, 100GB, 1TB)
IPFS_STORAGE_MAX=100GB

# IPFS data path (inside container)
IPFS_PATH=/data/ipfs

# ============================================
# FRW Configuration
# ============================================

# FRW IPFS API connection
# Use DNS name for docker-compose, IP for standalone
FRW_IPFS_API=/dns/ipfs/tcp/5001
# FRW_IPFS_API=/ip4/127.0.0.1/tcp/5001

# Node environment
NODE_ENV=production

# ============================================
# FRW Gateway Configuration
# ============================================

# Gateway HTTP port
FRW_GATEWAY_PORT=3000

# Enable CORS for browser access
GATEWAY_CORS_ENABLED=true

# Allowed origins (comma-separated)
GATEWAY_CORS_ORIGINS=*

# Cache duration (seconds)
GATEWAY_CACHE_TTL=3600

# ============================================
# Logging Configuration
# ============================================

# Log level (debug, info, warn, error)
LOG_LEVEL=info

# Enable debug mode for FRW
# DEBUG=frw:*

# ============================================
# Security Configuration
# ============================================

# Bind API to localhost only (recommended for production)
IPFS_API_BIND=127.0.0.1

# Enable authentication for gateway (future)
# GATEWAY_AUTH_ENABLED=false
# GATEWAY_API_KEY=your-secret-key-here

# ============================================
# Performance Configuration
# ============================================

# Connection limits
IPFS_CONN_MGR_LOW=200
IPFS_CONN_MGR_HIGH=400
IPFS_CONN_MGR_GRACE=30s

# Datastore configuration
IPFS_DATASTORE_SPEC=leveldb

# ============================================
# Development Configuration
# ============================================

# Hot reload for development
# CHOKIDAR_USEPOLLING=true

# Expose debug port
# NODE_DEBUG_PORT=9229

# ============================================
# Custom Configuration
# ============================================

# Custom IPFS bootstrap nodes (comma-separated)
# IPFS_BOOTSTRAP_NODES=/ip4/1.2.3.4/tcp/4001/p2p/QmXXX...

# Custom FRW registry server (future)
# FRW_REGISTRY_URL=https://registry.frw.example.com

# ============================================
# Volume Mount Points (Host Paths)
# ============================================

# Host directory for published sites
# LOCAL_SITES_PATH=./sites

# Host directory for IPFS data
# LOCAL_IPFS_DATA=/var/lib/frw/ipfs

# Host directory for FRW config
# LOCAL_FRW_CONFIG=/etc/frw
